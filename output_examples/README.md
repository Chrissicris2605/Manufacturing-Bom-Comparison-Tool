# Output examples

Run the demo to generate a formatted Excel report in this folder:

```bash
python src/main.py sample_data/old_bom.csv sample_data/new_bom.csv output_examples/delta_report.xlsx
```

Generated `.xlsx` files are ignored by Git so the repository stays lightweight.
